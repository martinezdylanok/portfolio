name: Test

on: pull_request

jobs:
   basic_job:
      runs-on: ubuntu-latest
      container: node:24-alpine
      steps:
         - uses: actions/checkout@v5
         - run: npm ci
         - run: npm run lint
         - run: npm run test
         - run: npm run build