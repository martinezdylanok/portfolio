import { CONTACT_FORM_TITLE } from "./data/contactFormTitleData";

const ContactFormTitle = () => {
   return <h2 className="text-8xl font-hanken-grotesk font-bold mb-12 text-heading contact-form__title">{CONTACT_FORM_TITLE}</h2>;
};

export default ContactFormTitle;
